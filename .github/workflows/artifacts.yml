name: Artifacts

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  create:
    runs-on: ubuntu-latest

    steps:
      - name: create file
        run: |
          cat > file <<EOF
          hello
          world
          EOF
      - name: upload
        uses: actions/upload-artifact@v3
        with:
          name: test
          path: file

      - name: Cleanup
        run: |
          rm -f file

      - name: retrieve artifact
        uses: actions/download-artifact@v3
        with:
          name: test
      
      - name: Review
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cat file
          rm -f file
          gh -R "$GITHUB_REPOSITORY" run download -n test
          cat file

  retrieve:
    runs-on: ubuntu-latest
    needs: create

    steps:
      - name: retrieve artifact
        uses: actions/download-artifact@v3
        with:
          name: test
      
      - name: Review
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cat file
          rm -f file
          gh -R "$GITHUB_REPOSITORY" run download -n test
          find .
          cat file
